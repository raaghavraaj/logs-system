spring.application.name=distributor

# Enable debug logging for our distribution algorithm
logging.level.com.resolveai.distributor.services.DistributorServiceImpl=DEBUG

# Metrics and Monitoring Configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus,env,beans,loggers
management.endpoint.health.show-details=always
management.endpoint.metrics.enabled=true
management.endpoint.prometheus.enabled=true
management.metrics.export.prometheus.enabled=true
management.metrics.web.server.request.autotime.enabled=true
management.info.env.enabled=true

# Application Information
info.app.name=Logs Distributor Service
info.app.version=1.0.0
info.app.description=High-throughput logs distribution system with weighted load balancing
